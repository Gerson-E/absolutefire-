"""
Optional image annotation / pre-processing utilities.

Owner: Owner 2 — Backend

TODO (future): draw bounding boxes on the image and return an annotated
image URL or base64 string for display in the frontend.

Currently unused — annotated_image_url is always null in the response.
"""
