/**
 * ErrorBanner — displays an error message with a dismiss action.
 *
 * Owner: Owner 1 — Frontend
 *
 * Props:
 *   message: string
 *   onDismiss: () => void
 *
 * TODO: implement
 */

export function ErrorBanner(_props: {
  message: string;
  onDismiss: () => void;
}) {
  return <div>TODO: ErrorBanner</div>;
}
