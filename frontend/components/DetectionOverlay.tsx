/**
 * DetectionOverlay — renders bounding boxes over the uploaded image
 * using a <canvas> or absolutely-positioned divs.
 *
 * Owner: Owner 1 — Frontend
 *
 * Props:
 *   imageUrl: string
 *   detections: Detection[]   (from AnalyzeResponse)
 *
 * TODO: implement
 */

import type { Detection } from "@/lib/types";

export function DetectionOverlay(_props: {
  imageUrl: string;
  detections: Detection[];
}) {
  return <div>TODO: DetectionOverlay</div>;
}
