/**
 * UploadForm — image file picker + optional px_per_mm field + submit.
 *
 * Owner: Owner 1 — Frontend
 *
 * Props:
 *   onSubmit(file: File, pxPerMm?: number) => void
 *
 * TODO: implement
 */

export function UploadForm(_props: {
  onSubmit: (file: File, pxPerMm?: number) => void;
}) {
  return <div>TODO: UploadForm</div>;
}
